@using TegCMS.Pages.Models
@model TegCMS.Pages.Models.PageViewModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <header>
        @Html.Action("Index", "Html", new RouteValueDictionary { { "configuration", new HtmlConfiguration { Html = "<h1>Another test</h1>" } } })
    </header>
    
    <div>
        @Html.Action("Index", "Html", new RouteValueDictionary { { "configuration", new HtmlConfiguration { Html = "<article><h2>One</h2> One, Two, Three</article>" } } })
        
        @Html.Action("Index", "Html", new RouteValueDictionary { { "configuration", new HtmlConfiguration { Html = "<article><h2>Two</h2> Four, Five, Six</article>" } } })
    </div>
</body>
</html>
